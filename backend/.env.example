# Backend HTTP server
PORT=4000

# Email configuration for approval emails
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=arpit232118@gmail.com
EMAIL_PASS=your-app-password
EMAIL_FROM=arpit232118@gmail.com

# API key for admin endpoints (keep secret, not in client public JS)
API_KEY=replace-with-a-strong-secret

# Admin login (only for demonstration; production should use hashed password + proper auth)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# Optional Twilio SMS configuration
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=
